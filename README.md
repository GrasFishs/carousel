# carousel
一个轮播组件
# 使用方式
```
new Carousel(ele,options);
/*
* ele:挂载的标签id
* options:{
* during:type{Number},//（可选），自动轮播切换时间，单位ms
* direction:type{String},//（可选）方向，默认横向；纵向则是'vertical'
* width:type{Number},//（可选）最大宽度，必须和height一起，单位px
* height:type{Number}//（可选）最大高度，必须和width一起，单位px
*}
*/
```
